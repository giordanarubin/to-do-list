<!DOCTYPE html>
<html lang="pt-br">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/style.css" />
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.13.1/font/bootstrap-icons.min.css"
    />
    <title>To do list</title>
  </head>

  <body>
    <% if (message != "") { %>
    <section class="message message-<%= type %>">
      <p><%= message %></p>
      <span id="close-message"><i class="bi bi-x-circle"></i></span>
    </section>
    <% } %>

    <header>
      <% if (task == null) { %>
      <form action="/create" method="POST">
        <!-- envia para a rota /create, definida em routes.js -->
        <h1>TO DO LIST</h1>
        <input
          type="text"
          name="task"
          id="task"
          placeholder="Insira uma tarefa"
        />
        <button id="addButton" type="submit">Adicionar</button
        ><!-- como o type é submit, ao clicar no botão ele executa a ação do form definida em "action" -->
      </form>

      <% } else { %>
      <h2>EDITAR TAREFA</h2>
      <form action="/updateOne/<%= task._id %>" method="POST">
        <input type="text" name="task" id="task" value="<%= task.task %>" />
        <button id="updateButton" type="submit">Atualizar</button>
      </form>
      <% } %>
    </header>

    <section id="tasks">
      <!-- iterando entre cada task para mostrar em tela. para mostrar valor, abre diferente o negocio do ejs. toLocaleDateString serve para formatar data p um padrao especifico -->
      <ul>
        <% tasksList.forEach((task) => { %>
        <li>
          <div>
            <a href="/"><%= task.task %></a>
            <span>Criado em <%= task.date.toLocaleDateString('pt-br') %></span>
          </div>
          <div>
            <a class="btnUpdate" href="/getById/<%= task._id %>/update"
              ><i class="bi bi-pencil-square"></i
            ></a>
            <a class="btnDelete" href="/getById/<%= task._id %>/delete"
              ><i class="bi bi-trash3"></i
            ></a>
          </div>
        </li>
        <% }) %>
      </ul>
    </section>

    <!-- deletar task -->
    <% if (taskDelete != null) {%>
    <section id="modal">
      <div id="card">
        <a id="close" href="/"><i class="bi bi-x-circle"></i></a>
        <h3>Confirmação</h3>
        <hr />
        <p>Deseja realmente apagar a tarefa: "<%= taskDelete.task %>"?</p>
        <div id="btns">
          <a id="btnNot" href="/">Não</a>
          <a id="btnYes" href="/deleteOne/<%= taskDelete._id %>">Sim</a>
        </div>
      </div>
    </section>
    <% } %>

    <script src="script.js"></script>
  </body>
</html>
